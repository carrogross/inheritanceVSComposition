import java.util.Random;

public class Composition {
    private Random random = new Random();

    void randomNumber(){
        System.out.println("Random int generated by class Composition: " + (random.nextInt()));
    }
}
